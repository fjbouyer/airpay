<% content_for :title, "Airpay - Profile page" %>

<div class="container text-center">
  <%= render 'account/tab-menu', dashboard_class: "tab active" %>
</div>

<div class="container text-center">
  <div class="tab-content" id="dashboard">
    <h2>Welcome <%= "#{current_user.first_name} #{current_user.last_name}" %></h2>
    <p>This is my dashboard.</p>
  </div>
</div>

<!-- To activate if we can do everything on time before, code in js file commented as well -->
<!-- <%= javascript_include_tag 'tabs' %> -->
